/* 项目首页组件 */

import React, { Component,Fragment } from 'react'  // 导入 React 依赖

// 创建 App 组件实例
class App extends Component {
  // 创建 组件实例 节点
  render() {
    // 返回 组件实例 节点
    return (
      // 组件节点
      <Fragment>
      <div>
        <h1>初始化项目</h1>
      </div>
      </Fragment>
    )
  }
}

// 导出 App 组件
export default App;
