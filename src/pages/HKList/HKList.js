/* 找房页面组件 */

import React, { Component, Fragment } from "react";  // 导入 React 依赖

// 创建 HKList 组件
class HKList extends Component {
  // 创建 组件节点
  render() {
    // 返回 组件节点
    return <Fragment>HKList</Fragment>;
  }
}

// 导出 HKList 组件
export default HKList;
